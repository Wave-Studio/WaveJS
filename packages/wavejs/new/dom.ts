/**
 * WaveJS
 *
 * @author Wave-studios
 */

// TODO: Make this work
const updateDom = (newUI: DocumentFragment) => {
	const oldUI = document.getElementById("wjs-app")!;
	const newUIChildren = newUI.childNodes;
	const oldUIChildren = oldUI.childNodes;
}